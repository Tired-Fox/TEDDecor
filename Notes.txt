Assign:
    - targets list(Name)

AnnAssign:
    - target (Name)
    - annotation (Name)

Attribute:
    - value (Name)
    - attr (str)
    
Subscript:
    - value (Name)
    - slice (Tuple, Name)

slice:
    - lower (Constant)
    - upper (Constant)


Check __all__ to see what is available with import 

Parse the docs according to Google standards.
Parse variable docs
Local variables (attributes)